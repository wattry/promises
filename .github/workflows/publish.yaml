# This workflow will run tests using node and then publish a package to GitHub Packages when a release is created
# For more information see: https://docs.github.com/en/actions/publishing-packages/publishing-nodejs-packages

name: Publish Package
on:
  push:
    branches: [main]
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://npm.pkg.github.com/
      # TODO(human): enable corepack here so `pnpm` resolves to the pinned
      # packageManager version (pnpm@11.3.0) from package.json
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - name: publish
        run: |
          CURRENT_VERSION=$(npm show @wattry/promises version 2>/dev/null || echo "")
          NEW_VERSION=$(node -p "require('./package.json').version")

          echo "current:  $CURRENT_VERSION"
          echo "new:      $NEW_VERSION"

          if [ "$CURRENT_VERSION" == "$NEW_VERSION" ]; then
            echo "No change in version skipping publish"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "v$NEW_VERSION"
          git push origin "v$NEW_VERSION"
          pnpm publish
        env:
          NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
